# 🔍 Анализ на Логическия Поток за Калории и Макроси

## 📋 Резюме на Проблема

Потребителят пита дали има логически пречки за изчисляване на правилните калории, макроси и грамажи в системата с 3-стъпков AI модел.

**Очаквана логика:**
1. Backend изчислява базови стойности (BMR, TDEE, базови калории, базови макроси)
2. AI Стъпка 1 (Analysis) преизчислява препоръчителни калории и макроси спрямо ВСИЧКИ фактори
3. AI Стъпка 2 (Strategy) определя стратегия и калории/макроси за всеки ден поотделно (може да варира)
4. AI Стъпка 3 (Menu) използва данните от Стъпка 2 за създаване на менюто

**Въпрос:** Има ли места където правилните калории/макроси избрани от AI могат да се объркат?

---

## ✅ Какво Работи ПРАВИЛНО

### 1. Backend → Step 1 (Analysis)

**Backend изчисления (worker.js, редове 244-486):**
```javascript
// Backend изчислява базови стойности
const bmr = calculateBMR(data);
const tdee = calculateTDEE(bmr, activityScore);
const deficitData = calculateSafeDeficit(tdee, goal);
const macros = calculateMacronutrientRatios(data, activityScore, tdee);
```

**Подават се към AI като РЕФЕРЕНЦИЯ:**
```javascript
// worker.js, generateAnalysisPrompt, ~ред 4280
═══ BACKEND РЕФЕРЕНТНИ ИЗЧИСЛЕНИЯ ═══
- BMR (Mifflin-St Jeor): ${bmr} kcal
- TDEE: ${tdee} kcal
- БЕЗОПАСЕН ДЕФИЦИТ: ${targetCalories} kcal
  * Максимален дефицит: 25%
  * Стандартен дефицит: ${deficitPercent}%
- БАЗОВИ МАКРОНУТРИЕНТИ:
  * Protein: ${protein}%
  * Carbs: ${carbs}%
  * Fats: ${fats}%

РЕФЕРЕНТНИТЕ изчисления са само ОТПРАВНА ТОЧКА за валидация.
ТИ определяш финалните стойности според холистичния анализ.
```

**AI Стъпка 1 ПРЕИЗЧИСЛЯВА:**
```json
// Plan2, Step 1 Output
{
  "bmr": число,  // AI може да коригира ±10-20%
  "tdee": число, // AI може да коригира
  "recommendedCalories": число, // AI определя финални калории
  "macroRatios": {
    "protein": процент, // AI определя финални проценти
    "carbs": процент,
    "fats": процент
  },
  "macroGrams": {
    "protein": грамове, // AI изчислява грамове
    "carbs": грамове,
    "fats": грамове
  }
}
```

**Вердикт:** ✅ РАБОТИ КОРЕКТНО
- Backend дава базови стойности като референция
- AI ги използва като отправна точка и преизчислява според всички фактори
- AI има свободата да коригира според метаболитен профил, здраве, психология и др.

---

### 2. Step 1 → Step 2 (Strategy)

**Какво се подава към Step 2 (worker.js, ~ред 4540):**
```javascript
const analysisCompact = {
  bmr: analysis.bmr,
  tdee: analysis.tdee,
  recommendedCalories: analysis.recommendedCalories,
  macroRatios: "Protein: X%, Carbs: Y%, Fats: Z%",
  macroGrams: "Protein: Xg, Carbs: Yg, Fats: Zg",
  ...
}
```

**Plan2, Step 2 вижда:**
```
АНАЛИЗ (КОМПАКТЕН):
- BMR/TDEE/Калории: {bmr} / {tdee} / {recommendedCalories}
- Макро съотношения: {macroRatios}
- Макро грамове дневно: {macroGrams}
```

**AI Стъпка 2 изхожда от Step 1 стойности:**
```json
// Plan2, Step 2 Output
{
  "calorieDistribution": "текст - как се разпределят калориите по дни и хранения",
  "macroDistribution": "текст - как се разпределят макросите според дни/хранения",
  "weeklyScheme": {
    "monday": {
      "meals": 3,
      "description": "база +0% спрямо {recommendedCalories}, балансирани макроси"
    },
    "tuesday": {
      "meals": 3, 
      "description": "+10% калории (carb cycling - висок ден)"
    },
    ...
  }
}
```

**Вердикт:** ✅ РАБОТИ КОРЕКТНО
- Step 2 получава AI-изчислените стойности от Step 1
- Базира калориите и макросите на {recommendedCalories} от Step 1
- Може да варира по дни (напр. +10%, -10%, carb cycling)

---

## ⚠️ ПОТЕНЦИАЛЕН ПРОБЛЕМ: Step 2 → Step 3

### Проблем #1: Текстово описание вместо конкретни числа

**Step 2 изход:**
```json
{
  "calorieDistribution": "Понеделник-Петък: базови калории (1800 kcal), Събота: +10% (1980 kcal), Неделя: свободно хранене",
  "macroDistribution": "Понеделник, Сряда, Петък: високо протеин (35%), Вторник, Четвъртък: високи въглехидрати (50%)",
  "weeklyScheme": {
    "monday": {
      "meals": 3,
      "description": "база, балансирани макроси (30/40/30)"
    }
  }
}
```

**Step 3 получава (worker.js, ~ред 1618-1619):**
```javascript
Разпределение на калории (стъпка 2): ${strategyCompact.calorieDistribution}
Разпределение на макроси (стъпка 2): ${strategyCompact.macroDistribution}
```

**И също (ред 1600):**
```javascript
Цел: ${data.goal} | BMR: ${bmr} | Калории: ${recommendedCalories} kcal/ден | Модификатор: "${dietaryModifier}"
```

**План2, Step 3 инструкции:**
```
1. Разпределение на калории: Използвай "Разпределение на калории" от стъпка 2 
   за правилно разпределение на калориите по хранения за всеки от дните.

2. Макроси ЗАДЪЛЖИТЕЛНИ: protein, carbs, fats, fiber в грамове за ВСЯКО ястие.
   Изчисли ги през 50гр изчислени спрямо калориите заложени от стъпка 2 
   за съответното хранене и ден

3. Калории: определени са за всяко хранене и всеки ден от стъпка 2 calorieDistribution

4. Целеви дневни калории: разпределени са в стъпка 2 calorieDistribution
```

**ПРОБЛЕМ:**
- Step 3 получава **два източника** на калории:
  1. `${recommendedCalories}` - ЕДНО число от Step 1 (напр. 1800 kcal)
  2. `${strategyCompact.calorieDistribution}` - ТЕКСТОВО описание от Step 2
  
- Ако Step 2 описва различни калории за различни дни (напр. carb cycling), това е в **ТЕКСТ**, не в **числа**

- AI на Step 3 трябва да **парсва текста** и да разбере:
  - Понеделник: 1800 kcal
  - Вторник: 1980 kcal (+10%)
  - Неделя: свободно
  
- Това е податливо на грешки при интерпретация!

---

### Проблем #2: weeklyScheme.{day}.description е ТЕКСТ, не структурирани данни

**Step 2 изход:**
```json
{
  "weeklyScheme": {
    "monday": {
      "meals": 3,
      "description": "база +0%, балансирани макроси 30/40/30"
    },
    "tuesday": {
      "meals": 3,
      "description": "+10% калории, високи въглехидрати 25/50/25"
    }
  }
}
```

**ПРОБЛЕМ:**
- `description` е свободен текст, не структуриран JSON
- AI на Step 3 трябва да парсва:
  - "база +0%" → 1800 kcal
  - "+10% калории" → 1980 kcal
  - "балансирани макроси 30/40/30" → protein 30%, carbs 40%, fats 30%
  
- Различни AI модели могат да интерпретират по различен начин!

---

### Проблем #3: Липсва валидация между Step 2 и Step 3

**Сегашен код (worker.js, ~ред 4850-4880):**
```javascript
// Parse BMR and calories from STEP 1 (analysis)
let bmr = analysis.bmr || calculateBMR(data);
let recommendedCalories = analysis.recommendedCalories || fallback;

// Pass to Step 3
const chunkPrompt = await generateMealPlanChunkPrompt(
  data, analysis, strategy, 
  bmr, recommendedCalories,  // От Step 1!
  startDay, endDay, previousDays, env
);
```

**ПРОБЛЕМ:**
- `recommendedCalories` е от **Step 1**, не от **Step 2**
- Ако Step 2 решил че трябва различни калории за различни дни, това НЕ СЕ отразява в параметрите
- Step 3 разчита че AI ще прочете текста от `calorieDistribution`

---

## 🚨 РЕАЛНИ СЦЕНАРИИ ЗА ОБЪРКВАНЕ

### Сценарий #1: Carb Cycling

**Step 1 (Analysis):**
```json
{
  "recommendedCalories": 1800
}
```

**Step 2 (Strategy):**
```json
{
  "calorieDistribution": "Пон/Ср/Пет: 1600 kcal (low carb), Вт/Чет/Съб: 2000 kcal (high carb), Нд: свободно",
  "weeklyScheme": {
    "monday": {"meals": 3, "description": "-10% калории, ниски въглехидрати"},
    "tuesday": {"meals": 3, "description": "+10% калории, високи въглехидрати"}
  }
}
```

**Step 3 получава:**
```
Калории: 1800 kcal/ден  ← От Step 1
Разпределение на калории (стъпка 2): Пон/Ср/Пет: 1600 kcal...  ← От Step 2
```

**Въпрос:** Кои калории да използва AI на Step 3?
- Ако следва `recommendedCalories` → 1800 за всички дни ❌
- Ако следва `calorieDistribution` текст → трябва да парсва ✅ (но рисковано)

---

### Сценарий #2: Различни макроси за различни дни

**Step 1 (Analysis):**
```json
{
  "macroRatios": {"protein": 30, "carbs": 40, "fats": 30}
}
```

**Step 2 (Strategy):**
```json
{
  "macroDistribution": "Пон/Ср/Пет: 35/30/35 (високо протеин, low carb), Вт/Чет: 25/50/25 (high carb)"
}
```

**Step 3 получава:**
```
Макро съотношения: Protein: 30%, Carbs: 40%, Fats: 30%  ← От Step 1
Разпределение на макроси (стъпка 2): Пон/Ср/Пет: 35/30/35...  ← От Step 2
```

**Въпрос:** Кои макроси да използва AI?

---

### Сценарий #3: Свободно хранене в неделя

**Step 2 (Strategy):**
```json
{
  "weeklyScheme": {
    "sunday": {"meals": 3, "description": "свободно хранене на обяд, лека вечеря"}
  }
}
```

**Въпрос:** Колко калории за неделя? Step 2 казва "свободно" - това означава ли че Step 3 трябва да създаде:
- Менюто без калорични ограничения? 
- Двойно повече калории?
- Или само да маркира обяда като "свободен избор"?

---

## 🔧 ПРЕПОРЪКИ ЗА ПОДОБРЕНИЕ

### Опция A: Добави структурирани полета в Step 2 (ПРЕПОРЪЧИТЕЛНО)

**Промени Plan2, Step 2 JSON структура:**
```json
{
  "weeklyScheme": {
    "monday": {
      "meals": 3,
      "targetCalories": 1800,  // ← ДОБАВИ конкретно число
      "calorieModifier": 0,     // ← процент спрямо base (0%, +10%, -10%)
      "macroRatios": {          // ← ДОБАВИ конкретни проценти
        "protein": 30,
        "carbs": 40,
        "fats": 30
      },
      "description": "база, балансирани макроси"
    },
    "tuesday": {
      "meals": 3,
      "targetCalories": 1980,
      "calorieModifier": 10,
      "macroRatios": {
        "protein": 25,
        "carbs": 50,
        "fats": 25
      },
      "description": "+10% калории, високи въглехидрати (carb cycling)"
    }
  },
  "calorieDistribution": "текст - обяснение на логиката",
  "macroDistribution": "текст - обяснение на логиката"
}
```

**Промени worker.js за Step 3:**
```javascript
// За всеки ден, извлечи конкретните калории и макроси
const dayOfWeek = getDayOfWeek(dayNumber); // monday, tuesday, etc.
const dayScheme = strategy.weeklyScheme?.[dayOfWeek];

const targetCalories = dayScheme?.targetCalories || recommendedCalories;
const targetMacros = dayScheme?.macroRatios || analysis.macroRatios;
```

**Предимства:**
- ✅ Ясни, структурирани данни
- ✅ Няма нужда от парсване на текст
- ✅ По-малко грешки
- ✅ Лесно за валидация

**Недостатъци:**
- ❌ Изисква промяна в Plan2 промпт
- ❌ AI трябва да генерира допълнителни полета

---

### Опция B: Подобри инструкциите в Step 3 (ПО-ЛЕСНО)

**Промени Plan2, Step 3 инструкции:**
```
КРИТИЧНО ВАЖНО - КАЛОРИИ И МАКРОСИ:

1. ПРИОРИТЕТ НА ДАННИ:
   - ВИНАГИ следвай "Разпределение на калории (стъпка 2)" за всеки ден
   - "Калории: {recommendedCalories} kcal/ден" е БАЗОВА стойност от стъпка 1
   - Стъпка 2 може да промени калориите за различни дни (напр. carb cycling)
   
2. ПАРСВАНЕ НА calorieDistribution:
   - Ако текстът казва "база" или "базови калории" → използвай {recommendedCalories}
   - Ако текстът казва "+10%" → изчисли {recommendedCalories} × 1.10
   - Ако текстът казва "-10%" → изчисли {recommendedCalories} × 0.90
   - Ако текстът дава конкретно число (напр. "1600 kcal") → използвай това число
   
3. ПАРСВАНЕ НА macroDistribution:
   - Ако текстът дава проценти (напр. "35/30/35") → използвай тези проценти
   - Ако текстът казва "балансирани" → използвай макросите от стъпка 1
   - Ако текстът казва "високо протеин" → увеличи протеин до 35-40%
   
4. ПРОВЕРКА НА weeklyScheme.{day}.description:
   - Провери дневното описание за специфични указания за калории/макроси
   - То има приоритет над общото calorieDistribution описание
```

**Предимства:**
- ✅ Не изисква промяна в структурата
- ✅ Може да се имплементира само с промяна на промпта
- ✅ AI моделът получава ясни инструкции

**Недостатъци:**
- ❌ Все още разчита на AI да парсва текст
- ❌ Може да има вариации в интерпретацията

---

### Опция C: Добави валидация след Step 3 (ДОПЪЛНИТЕЛНО)

**Нов код във worker.js:**
```javascript
// След генериране на menu
function validateMenuCalories(menu, analysis, strategy) {
  const warnings = [];
  
  for (const [dayKey, dayData] of Object.entries(menu)) {
    const dayNumber = parseInt(dayKey.replace('day', ''));
    const dayOfWeek = getDayOfWeek(dayNumber);
    const dayScheme = strategy.weeklyScheme?.[dayOfWeek];
    
    // Check if daily totals match expected calories
    const actualCalories = dayData.dailyTotals?.calories;
    const expectedCalories = dayScheme?.targetCalories || analysis.recommendedCalories;
    
    const deviation = Math.abs(actualCalories - expectedCalories) / expectedCalories;
    
    if (deviation > 0.15) { // >15% deviation
      warnings.push({
        day: dayNumber,
        expected: expectedCalories,
        actual: actualCalories,
        deviation: (deviation * 100).toFixed(1) + '%'
      });
    }
  }
  
  return warnings;
}
```

---

## ✅ ЗАКЛЮЧЕНИЕ

### Отговор на въпроса: "Има ли логически пречки?"

**ДА, има потенциални пречки:**

1. **Step 3 получава два източника за калории:**
   - `recommendedCalories` (число от Step 1)
   - `calorieDistribution` (текст от Step 2)
   
2. **Step 2 описва варииране на калории/макроси в ТЕКСТ, не в структурирани данни**

3. **AI на Step 3 трябва да парсва текст за да разбере:**
   - Колко калории за всеки ден
   - Какви макроси за всеки ден
   - Как да разпредели по хранения

### Препоръчано решение:

**ОПЦИЯ A** (структурирани полета) е най-добрата, но изисква повече промени.

**ОПЦИЯ B** (подобрени инструкции) е по-бързо и лесно решение за момента.

**ОПЦИЯ C** (валидация) може да се добави като допълнителна проверка.

---

**Дата:** 2026-02-18  
**Статус:** ⚠️ Идентифицирани потенциални пречки  
**Препоръка:** Имплементирай Опция A или минимум Опция B  
**Версия:** 1.0
