=== PROFILE ===
Goal: {userData.goal} | BMR: {bmr} | Calories: {recommendedCalories} kcal/day | Modifier: "{dietaryModifier}"{modificationsSection}
Stress: {userData.stressLevel} | Sleep: {userData.sleepHours}h | Chronotype: {userData.chronotype}{previousDaysContext}

=== CONTEXT ===
{analysisBlock}
{strategyBlock}
Favorites: {dietLove} | Dislikes: {dietDislike}

=== DAY {startDay} (mealBreakdown) ===
{weeklySchemeByDayText}

{additionalNotes}
{clinicalProtocolSection}

=== CATALOG ===
{dynamicBlacklistSection}
{catalogSection}

=== FORMAT (slot types = mealBreakdown / #WK above, not this line) ===
{"type": "Хранене 2", "name": "Пилешко с ориз", "description": "• пилешки гърди 150g\n• ориз 80g\n• домати 100g"}

=== TASK (Day {startDay}) ===
Fill day {startDay} for {userData.name}. meals[].type MUST match this day's mealBreakdown exactly — no extra slots, no missing slots.
• type — from mealBreakdown
• name — dish title (Bulgarian)
• description — catalog raw ingredients only, one per line: "• {product} {N}g" (eggs not omelet; chicken not "rice with chicken")
Catalog products only; exact catalog names. Grams from kcal/P/C/F in mealBreakdown, round to 10g. No calories, macros, weight, benefits.

Macros: protein ≥20g → concentrated source; fats ≥10g → oil/nuts; carbs ≥25g → rice/potato/bread; high protein + low fat → lean sources.
Combinations: 2–6 products; one PRO + ENG if needed + veg; one of rice/potato/bread per meal; variety vs previous days{sweetsCravingRule}{freeMealInstruction}

=== MEAL TYPES ===
• Хранене 2/4 — main meals (meat, fish, eggs, cooked combinations)
• Хранене 3 — light snack only: fruit, raw nuts, skyr or yogurt. NO meat, fish, cooked dishes, rice, bread, pasta.
• Хранене 5 — fats+protein only (skyr, nuts, yogurt), max {MAX_LATE_SNACK_CALORIES} kcal
• Свободно хранене — only if strategy plans it this day: {"type":"Свободно хранене","name":"Свободно хранене"} NO description/calories/macros/weight/dessert. Do not generate Хранене 2 that day.
• "dessert": true — only on Хранене 2 when sweets needed; part of lunch, not a separate meal, not in name:
  {"type": "Хранене 2", "name": "Пилешко със салата", "description": "• ...", "dessert": true}

Return ONLY JSON for day {startDay}.
