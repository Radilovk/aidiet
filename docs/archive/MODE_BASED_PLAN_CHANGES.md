# Режим-базирана Система за Промени в Плана

## Проблем

Преди тази промяна, AI ботът правеше директни промени в хранителния план дори когато потребителят задаваше въпроси. Това водеше до объркване, защото ботът интерпретираше въпросите като заявки за промяна.

## Решение

Внедрихме система с два режима на работа:

### 1. Режим "Консултация" (по подразбиране)

**Характеристики:**
- Ботът може да чете плана и да отговаря на въпроси
- НЕ МОЖЕ да променя плана
- Ако потребителят иска промяна, ботът обяснява че трябва да активира режима за промяна

**Примери за отговори:**
```
Потребител: "Каква е закуската ми?"
Бот: "Закуската ти съдържа овесени ядки с банан (350 калории)."

Потребител: "Искам да сменя закуската"
Бот: "За да променя плана, моля активирай бутона 'Промени план' в долната част на чата."
```

### 2. Режим "Промяна на План"

**Характеристики:**
- Ботът може да чете И да променя плана
- Анализира дали промяната е здравословна
- Прилага промени чрез [UPDATE_PLAN:...] инструкции
- След промяна, планът се актуализира автоматично

**Примери за отговори:**
```
Потребител: "Искам да сменя закуската с омлет"
Бот: "Отлична идея! Омлетът е богат на протеин. ✓ Промяната е приложена!"
[UPDATE_PLAN:{"weekPlan":{"day1":{"meals":[...новите ястия...]}}}]
```

## Техническа Имплементация

### Backend (worker.js)

1. **Нов параметър `mode` в `/api/chat`:**
   ```javascript
   const { message, userId, conversationId, mode } = await request.json();
   const chatMode = mode || 'consultation';
   ```

2. **Проверка на режима преди UPDATE_PLAN:**
   ```javascript
   if (updatePlanIndex !== -1 && chatMode === 'modification') {
       // Обработва UPDATE_PLAN само в режим modification
   }
   ```

3. **Различни промпт инструкции за всеки режим:**
   ```javascript
   function generateChatPrompt(userMessage, userData, userPlan, conversationHistory, mode = 'consultation') {
       if (mode === 'consultation') {
           // Инструкции за консултация - БЕЗ UPDATE_PLAN
       } else if (mode === 'modification') {
           // Инструкции за промяна - С UPDATE_PLAN
       }
   }
   ```

### Frontend (plan.html)

1. **Глобална променлива за режима:**
   ```javascript
   let chatMode = 'consultation'; // Default mode
   ```

2. **UI индикатор за текущия режим:**
   - Зелен кръг + "Режим: Консултация" (по подразбиране)
   - Червен кръг + "Режим: Промяна на план" (когато е активиран)

3. **Бутон за превключване:**
   - В режим консултация: Червен бутон "Промени план"
   - В режим промяна: Зелен бутон "Консултация"

4. **Функция `toggleChatMode()`:**
   ```javascript
   function toggleChatMode() {
       chatMode = chatMode === 'consultation' ? 'modification' : 'consultation';
       updateModeUI();
       addMessageToChat(modeMessage, 'assistant');
   }
   ```

5. **Автоматично връщане към консултационен режим:**
   - При затваряне на чата, режимът се нулира на 'consultation'

## Потребителски Интерфейс

### Визуален Индикатор

```
┌─────────────────────────────────────────────┐
│  AI Диетолог Асистент               [X]     │
├─────────────────────────────────────────────┤
│  🟢 Режим: Консултация    [Промени план]   │  ← Консултация
│                                             │
│  🔴 Режим: Промяна на план [Консултация]   │  ← Промяна
└─────────────────────────────────────────────┘
```

### Цветова схема

- **Консултационен режим:**
  - Зелен индикатор (🟢)
  - Червен бутон "Промени план"
  - Светло зелен фон на индикатора

- **Режим за промяна:**
  - Червен индикатор (🔴)
  - Зелен бутон "Консултация"
  - Светло червен фон на индикатора

## Как да Използвате

### За Консултация (по подразбиране):
1. Отворете чата
2. Задайте въпроси за вашия план
3. Ботът ще отговаря без да променя плана

### За Промяна на Плана:
1. Отворете чата
2. Кликнете на бутона "Промени план"
3. Потвърдете със системното съобщение
4. Заявете промяната която искате
5. Ботът ще анализира и приложи промяната
6. Когато сте готови, кликнете "Консултация" за връщане

## Сигурност

- ✅ Потребителят трябва **експлицитно** да активира режима за промяна
- ✅ Въпросите **не се интерпретират** като промени в консултационен режим
- ✅ Ботът **информира** потребителя за текущия режим
- ✅ Режимът се **нулира** при затваряне на чата

## Тестване

### Тест 1: Консултационен режим не променя плана
```
1. Отворете чата (консултационен режим по подразбиране)
2. Напишете: "Искам да сменя закуската"
3. Очаквано: Ботът ще каже да активирате режима за промяна
4. Проверете: Планът НЕ е променен
```

### Тест 2: Режим за промяна променя плана
```
1. Отворете чата
2. Кликнете "Промени план"
3. Напишете: "Искам да сменя закуската с омлет"
4. Очаквано: Ботът ще приложи промяната
5. Проверете: Планът е актуализиран с омлет
```

### Тест 3: Превключване между режими
```
1. Отворете чата
2. Кликнете "Промени план" → Червен индикатор
3. Кликнете "Консултация" → Зелен индикатор
4. Очаквано: Индикаторът и бутоните се променят правилно
```

### Тест 4: Нулиране на режима
```
1. Отворете чата
2. Кликнете "Промени план"
3. Затворете чата
4. Отворете чата отново
5. Очаквано: Режимът е "Консултация" (по подразбиране)
```

## Предимства

✅ **Предотвратява неволни промени** - Потребителят трябва да активира режима експлицитно

✅ **Ясна комуникация** - Визуален индикатор показва текущия режим

✅ **Лесно превключване** - Един бутон за превключване между режими

✅ **Безопасност** - Режимът се нулира при затваряне на чата

✅ **Интуитивен UX** - Цветова схема (зелен = безопасно, червен = внимание)

## Бъдещи Подобрения

- [ ] Добавяне на потвърждение преди промяна на плана
- [ ] История на промените в плана
- [ ] Undo/Redo функционалност
- [ ] Експорт на промените

---

**Дата на внедряване:** 23 Декември 2024  
**Версия:** 2.1.0
