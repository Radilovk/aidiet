# Бърз Отговор: Актуализация на Default Prompts

## ❓ Въпрос
**Сега при евентуална промяна на worker.js и промптовете вътре, ще се актуализира ли при кликване на "viewDefaultPrompt" бутона и в админ панела?**

---

## ✅ Отговор
# ДА!

Промптовете ще се актуализират автоматично, **НО само след редеплоймънт**.

---

## 🚀 Как да актуализирам промптите?

### 3 Прости Стъпки:

```bash
# 1️⃣  Редактирай worker.js (lines 4447-5048)
vim worker.js

# 2️⃣  Deploy в Cloudflare
wrangler deploy

# 3️⃣  Готово! Промптите са актуализирани ✅
```

---

## ⏱️ След колко време?

- **Deploy време:** ~30 секунди
- **Разпространение:** 5-30 секунди
- **Общо:** ~1 минута максимум

---

## 🔍 Как да проверя?

### В Admin Panel:
1. Отвори https://radilov-k.github.io/aidiet/admin.html
2. Кликни "Виж Стандартен Промпт" (синия бутон)
3. ✅ Новият промпт се показва веднага!

### От командна линия:
```bash
curl https://aidiet.radilov-k.workers.dev/api/admin/get-default-prompt?type=analysis | jq -r '.prompt' | head -5
```

---

## 📋 Кратки факти

| Въпрос | Отговор |
|--------|---------|
| Актуализират ли се автоматично? | ✅ ДА, след deploy |
| Трябва ли да рестартирам нещо? | ❌ НЕ |
| Трябва ли да изчистя кеш? | ❌ НЕ |
| Колко време отнема? | ⏱️ ~1 минута |
| Ще се счупи ли нещо? | ❌ НЕ (ако синтаксисът е правилен) |
| Виждат ли всички потребители промяната? | ✅ ДА, веднага |

---

## 🎯 Важни точки

### ✅ ПРАВИЛНО (работи):
```
1. Edit worker.js
2. wrangler deploy
3. Click "View Default Prompt"
→ ✅ Вижда се новият промпт
```

### ❌ ГРЕШНО (не работи):
```
1. Edit worker.js
2. (не прави deploy)
3. Click "View Default Prompt"
→ ❌ Вижда се старият промпт
```

---

## 🔑 Ключова информация

### Откъде идват промптите?
Промптите са **hardcoded** в `worker.js` на редове **4447-5048**.

```javascript
const defaultPrompts = {
  analysis: `Ти си експертен диетолог...`,      // 159 реда
  strategy: `Базирайки се на здравословния...`,  // 130 реда
  meal_plan: `Ти действаш като ADLE...`,        // 235 реда
  summary: `Създай summary...`,                  // 29 реда
  consultation: `ТЕКУЩ РЕЖИМ: КОНСУЛТАЦИЯ...`,   // 14 реда
  modification: `ТЕКУЩ РЕЖИМ: ПРОМЯНА...`        // 80 реда
};
```

### Защо работи така?
- Cloudflare Worker изпълнява кода при **всяка заявка**
- **Няма кеширане** на промптите
- **Няма база данни** за default промптите
- Промптите са **част от кода**

---

## 🛠️ Пълен пример

### Сценарий: Искам да добавя нова инструкция в analysis промпта

```bash
# Стъпка 1: Редактирай
vim worker.js
# Go to line 4448
# Add: "НОВА ИНСТРУКЦИЯ: Винаги проверявай..."

# Стъпка 2: Валидирай (опционално)
node --check worker.js
# ✅ No errors

# Стъпка 3: Deploy
wrangler deploy
# ✅ Published aidiet-worker
#    https://aidiet.radilov-k.workers.dev

# Стъпка 4: Тествай
curl https://aidiet.radilov-k.workers.dev/api/admin/get-default-prompt?type=analysis | grep "НОВА ИНСТРУКЦИЯ"
# ✅ НОВА ИНСТРУКЦИЯ: Винаги проверявай...

# Стъпка 5: Провери в Admin Panel
# Open admin.html → Click "View Default Prompt"
# ✅ Виждаш "НОВА ИНСТРУКЦИЯ: Винаги проверявай..."
```

---

## 🔄 Какво ако искам да върна старата версия?

```bash
# 1. Revert промените в worker.js
git checkout worker.js

# 2. Deploy старата версия
wrangler deploy

# 3. Готово! Старата версия е активна ✅
```

---

## 📞 Поддръжка

### Ако нещо не работи:

1. **Провери дали deploy-а е успешен:**
   ```bash
   wrangler deploy
   # Look for: ✅ Published aidiet-worker
   ```

2. **Провери дали промптът се връща от API:**
   ```bash
   curl https://aidiet.radilov-k.workers.dev/api/admin/get-default-prompt?type=analysis
   ```

3. **Провери JavaScript синтаксиса:**
   ```bash
   node --check worker.js
   ```

4. **Погледни логовете:**
   ```bash
   wrangler tail
   ```

---

## 📚 Допълнителна документация

- **Пълна документация:** `DEFAULT_PROMPT_UPDATE_BEHAVIOR.md`
- **Визуално ръководство:** `DEFAULT_PROMPT_UPDATE_VISUAL_BG.md`
- **Deployment guide:** `DEPLOYMENT_CHECKLIST.md`
- **Worker документация:** `WORKER_README.md`

---

## ✨ Резюме

```
┌─────────────────────────────────────────────┐
│                                             │
│  1. Редактирай worker.js (lines 4447-5048) │
│             ↓                               │
│  2. wrangler deploy                         │
│             ↓                               │
│  3. Изчакай ~1 минута                       │
│             ↓                               │
│  4. ✅ Промптите са актуализирани!          │
│                                             │
│  🎯 Click "View Default Prompt" и виж       │
│     новия промпт веднага!                   │
│                                             │
└─────────────────────────────────────────────┘
```

---

**Версия:** 1.0  
**Дата:** 2026-02-05  
**Статус:** ✅ Актуално и тествано
